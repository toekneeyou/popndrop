<% if current_user.id == params[:id].to_i%>
  <%= render 'show_current' %>
  <!-- display user's toilets, list a new toilet, and current bookings -->
<% else %>
  <%= render 'show_other' %>
  <!-- display user's info and toilets. no toilet form or current bookings. -->
<% end %>
