<%= simple_form_for [@booking @review] do |f| %>
<%= f.input :reviewable, collection: (Toilet.find(@booking.toilet_id) %>
<%= f.input :rating, inclusion: { in: (0..5).to_a } %>
<%= f.input :content %>
<%= f.submit %>
<% end %>
